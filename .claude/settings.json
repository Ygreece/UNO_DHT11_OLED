{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "repo='/mnt/c/Users/greece/Desktop/UNO_DHT11_OLED'; file=$(jq -r '.tool_input.file_path // .tool_response.filePath // empty'); case \"$file\" in \"$repo\"/*) ;; *) exit 0 ;; esac; cd \"$repo\" || exit 1; git add -A -- \"$file\" && git diff --cached --quiet -- \"$file\" && exit 0; git commit --only -m 'chore: auto-save changes' -- \"$file\" && git push origin HEAD",
            "timeout": 120,
            "statusMessage": "提交并推送修改"
          }
        ]
      }
    ]
  }
}
